# 2026-04-17 日志

## OpenClaw 升级后脚本丢失问题

用户反映升级后定时任务/提醒全没了。

### 当前状态

**Crontab 里的失效引用（需清理）：**
```
/Users/ice/.openclaw/scripts/                    ← 目录不存在
/Users/ice/.openclaw/workspace/scripts/          ← 目录不存在
```
涉及的失效脚本：
- `polymarket_monitor.py`
- `refresh-and-sync-token.sh`
- `lcm-to-qmd-sync.sh`
- `soul-guardian-cron.sh`
- `research_director.sh`（实际在 QuantaAlpha 里）
- `healthcheck.sh`
- `daily_sync_cron.sh`（实际在 QuantaAlpha 里）

**实际脚本位置（未丢失）：**
- `/Users/ice/Document/QuantaAlpha/scripts/` 里包含了：
  - `research_director.sh`
  - `daily_sync_cron.sh`
  - `fetch_ercot_lmp.py`（ERCOT 德州电力监控）

**Crontab 里没有 ERCOT 定时任务**，用户说"美国电力监控也没了"——可能是指原来有但没在 crontab 里，或者是个独立 daemon。

### 用户还在确认
- 美国电力监控具体是哪个脚本/daemon
- 那些失效的 crontab 行是否需要删除并重建

### 建议
1. 用户应运行 `crontab -e` 删除失效行
2. 确认 ERCOT 脚本是否有对应的 cron 条目需要重建
3. 确认其他丢失的提醒/cron 的具体用途

## 今日完成
- REPL agent 研究：找到 A-R Behavioral Space (2026)、CES (2025)、NExT (2024) 等论文
- 分析了 semilens.vercel.app API（半导体 beta 数据、实时行情）
- 总结了播客"专业的交易必须建立在假设之上"的转录

## 待处理
- Tushare Pro Token（financial-report-minesweeper 需要）
- "量化论文速读"B站视频时长问题（下载的只有135秒，原视频1h29m）
- "惊弓之鸟"论文搜索（需要用户提供标题/作者/DOI）

## Heartbeat 19:12 - 发现的问题

### Cron 脚本路径断裂（已修复2/3）
- lcm-sync: crontab 指向 `~/.openclaw/workspace/scripts/`（不存在），实际脚本在 `~/.openclaw/scripts/` → 已创建 symlink
- soul-guardian: 同上 → 已创建 symlink
- token-sync: 脚本存在且 crontab 正确，但 log 尚未创建（下次运行 20:00 验证）
- research-director: 19h 未运行（上次 00:00），4h 间隔，应该多次触发但日志无新条目 → 需关注

### quantaalpha 问题
- Token refresh ECONNREFUSED 127.0.0.1:7891（代理未运行？）
- gpt-5.2-codex model not supported（ChatGPT account 限制）
- exec preflight 阻止 datacenter-monitor 虚拟环境激活脚本

### OpenClaw context engine
- lossless-claw factory 注册失败，fallback 到 legacy engine

### 已知无需处理
- Perplexity API 402 — 已在处理中

## Heartbeat 20:19

### Cron Health Issues
- **lcm-sync**: BROKEN - symlink `workspace/scripts/lcm-to-qmd-sync.sh` → `/Users/ice/.openclaw/scripts/lcm-to-qmd-sync.sh` (target missing). Cron log fills with errors.
- **soul-guardian**: BROKEN - symlink `workspace/scripts/soul-guardian-cron.sh` → `/Users/ice/.openclaw/scripts/soul-guardian-cron.sh` (target missing). Same issue.
- **token-sync**: Log missing - cron runs `refresh-and-sync-token.sh` but no `token-sync.log` produced. Need to verify logging.
- **research-director**: OK (ran today 00:00)

### Agent Health
- **main**: Active 20:19 ✓
- **quantaalpha**: Active 19:52 ✓, A股 data: china-energy updated 20:05 ✓

### Self-healing
- **SearXNG**: Had 2 consecutive failures, restarted automatically. Recovered ✓
- **EIA API**: SSL EOF errors (upstream/network issue). Not actionable locally.
- **interconnection.fyi**: SSL errors on all markets. External site issue.

### Action Items
- [ ] Fix lcm-to-qmd-sync.sh: actual script missing, need to recreate or restore
- [ ] Fix soul-guardian-cron.sh: actual script missing, need to recreate or restore
- [ ] Investigate token-sync logging (why no token-sync.log?)

## 修复记录 (2026-04-17 深夜)

### soul-guardian/lcm-sync cron 修复
- **根因**：之前 `cp` 备份脚本到 workspace/scripts 时用的是 broken symlink，导致复制出来的是空文件或循环引用
- **修复**：从 `.openclaw.pre-migration/workspace/scripts/` 重新复制原始脚本到 `~/.openclaw/workspace/scripts/`
- **验证**：soul-guardian cron 现在正常（log 显示 "ran at 02:27"），不再报 "No such file or directory"

### 脚本路径
- lcm-sync: `~/.openclaw/workspace/scripts/lcm-to-qmd-sync.sh`
- soul-guardian: `~/.openclaw/workspace/scripts/soul-guardian-cron.sh`
- 两者均由 crontab（而非 OpenClaw cron）触发：
  - `0 * * * *` lcm-sync
  - `*/30 * * * *` soul-guardian

### token-sync 修复
- `sync-anthropic-token.sh` 里的 inline Python 有 bug（KeyError: 'anthropic:oauth'）
- 修复：改用 `sync-anthropic-token.py` wrapper
- 验证：log 显示 "Updated agents: quantaalpha, fix, coder"

### Gateway 状态
- HTTP 响应正常 (curl to 18789)
- 但 `pgrep -f openclaw-gateway` 间歇性找不到进程（可能是权限问题）
- 日志文件正常写入

### 遗留问题
- Gateway CLI (`openclaw` 命令) 偶发 "pairing required" 错误，但 gateway 本身在跑
- Soul Guardian (clawsec/soul_guardian.py) 未安装，脚本会静默跳过检查
