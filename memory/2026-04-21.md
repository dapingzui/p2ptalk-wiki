# 2026-04-21 Daily Notes

## Heartbeat 14:17

### Cron Status (All OK)
- token-sync: 14:00 ✅
- research-director: 14:00 ✅
- lcm-sync: 14:00 ✅
- soul-guardian: 14:00 ✅ (detected MEMORY.md drift - normal)

### Agents
- main: active (session 14:17)
- quantaalpha: active (session 13:55)

### Self-Healing
- **mihomo proxy exhausted**: quantaalpha cron 03:00 failed with multiple EIA API errors - connection pool depleted (many CLOSE_WAIT connections)
- **Fix**: `kill -HUP 47572` (mihomo sidecar PID) → proxy recovered, tested successfully
- **Root cause**: ~2-day uptime, proxy connection pool maxed out

### Desktop
- Clean (empty)

### Issue to Monitor
- quantaalpha grid_demand_fetch.py still using direct requests + HTTP_PROXY instead of MCP alpha-vantage-mcp
  - MCP server should be more resilient; worth migrating the script to use MCP
  - Filed as consideration in proactive-ideas.md

## Heartbeat 15:20

### Cron Status (All OK @ 15:00)
- token-sync ✅
- research-director ✅  
- lcm-sync ✅
- soul-guardian ✅ (alert: MEMORY.md drift, TOOLS.md drift, HEARTBEAT.md drift)

### Agents
- main: active (session 15:20)
- quantaalpha: last session 14:53 (~27m ago) - ok

### Self-Healing
- **EIA API proxy errors @ 03:00**: Multiple EIA endpoints failed with `Connection refused` on port 7890
  - Root cause: proxy was down at 3 AM, currently UP (tested 200)
  - Grid demand data still from Apr 19 (2 days stale)
  - Not mihomo exhaustion this time - proxy itself was down
- Soul-guardian flagged drift on MEMORY.md, TOOLS.md, HEARTBEAT.md (all normal activity changes)

### Desktop
- Clean (empty)

### Monitoring
- Grid demand data 2 days stale - proxy downtime at 03:00 prevented daily refresh
- 发现 NPA 每日信号存在严重 freshness 问题：`narrative_daily` 底层库最新数据仅到 `2026-03-17`，但日报标题被包装为 `2026-04-22`，属于用旧数据冒充当日信号。
- 已验证旧值与报告内容匹配（如 `semiconductor`/`quantum`/`brain_computer` 的 stage 和收益），说明问题根因不是单纯算子错误，而是数据管道停更且缺少 freshness guard。
- 后续应优先修两件事：1) 找出未更新 `narrative_daily` 的 cron/脚本；2) 在 NPA 日报生成链路增加 freshness 校验，超过 1 个交易日禁止发信号。

## Heartbeat 17:17
- All 4 cron tasks ran at 17:00 ✓ (token-sync, research-director, lcm-sync, soul-guardian)
- Soul-guardian: MEMORY.md drift [alert] - expected, changes from this session
- main agent: active (session at 17:17)
- quantaalpha agent: active (session at 16:58)
- Clash proxy: alive and listening on 7890/7891/7892
- EIA API failures in watchdog log: from 03:00 AM run when proxy was likely down, not current
- Context engine issue: lossless-claw ID mismatch, falling back to legacy (non-critical)
- No action taken on context engine (needs plugin update, not heartbeat-level fix)

## Heartbeat 2026-04-21 18:17 (CST)

### 自愈检查发现
- **EIA API 批量失败**: quantaalpha 的 grid_demand_fetch 全部报 `Connection refused` 到 127.0.0.1:7890。根因：代理（Clash Verge）端口 7890 未运行。持续性问题。
- **soul-guardian 持续报告 MEMORY.md drift**: 每30分钟 alert 一次，可能是 soul-guardian 基线对比的是旧 hash，需要重新基线化。

### cron 健康
- token-sync ✓
- research-director ✓
- lcm-sync ✓
- soul-guardian ✓ (但有重复 drift alert)

### quantaalpha 数据
- EIA API 因代理 down 全部 FAIL

## Heartbeat 19:17

### Cron Status (All OK @ 19:00)
- token-sync ✅, research-director ✅, lcm-sync ✅, soul-guardian ✅

### Agents
- main: active (session 19:17)
- quantaalpha: last session 18:56 (~1h stale)

### Self-Healing
- Old EIA/proxy errors (03:00 today) confirmed stale - proxy was exhausted, earlier 14:17 heartbeat already fixed
- No new FAIL errors in current cycle
- Embedded agent timeout @ 18:30: MiniMax-M2.7 one-shot (30s), no cascade

### Data Status
- EIA via proxy: still failing (api.eia.gov unreachable through mihomo - likely geo/upstream block)
- interconnection.fyi via proxy: OK
- grid-demand data: stale since Apr 17

### Exec Preflight Blocks (Known)
Scripts blocked by preflight (cd/source && python chains):
- datacenter-monitor, gen_kg_html2.py, inkwell_fetch.py, extract_memory_cards.py, SEC EDGAR watcher
- Fix: rewrite as `python3 /full/path/to/script.py` or shell wrapper

### Proactive Idea
- grid_demand_fetch.py has `direct_get()` in backfill_eia_api.py but doesn't use it; consider NO_PROXY bypass for EIA or migrate to MCP alpha-vantage-mcp (already noted in proactive-ideas.md)


## Heartbeat 20:17

### P2PTalk Wiki — 修复 graph.html 多重 Bug

**问题**：动态图谱（graph.html）节点不显示，只显示"加载中"

**Bug #1 — SVG 未创建**：
- 症状：loading 一直转，节点/边全无
- 根因：`.attr('width', width)` 中 `width` 变量未声明（应为 `W`），JS 报错后 SVG 创建失败
- 修复：改为 `W`（来自 `dims = {w: W, h: H}`）

**Bug #2 — "node not found: 0"**：
- 症状：D3 forceLink 初始化时报错 `node not found: 0`
- 根因：backlink 统计代码先于 `forceLink` 执行，此时 `link.source` 已是 node 对象而非 integer index，`DATA.nodes[nodeObject]` 返回 undefined
- 修复：调整解析顺序 — 先做 backlink 统计，再将所有 link.source/target 转为 string ID，最后建 simulation

**Bug #3 — D3 CDN 不可达**：
- 修复：D3 库内嵌本地（279KB inline），不依赖外部 CDN

**修复脚本**：`/tmp/gen_graph_html.py`（已永久修复生成器）

**GitHub Pages**：
- Repo: `dapingzui/p2ptalk-wiki`
- URL: https://dapingzui.github.io/p2ptalk-wiki/
- 图谱页: https://dapingzui.github.io/p2ptalk-wiki/graph.html

**统计**：50 个概念页（47+3新增），88条双向链接，333条原始消息，68个线程

### 其他 Wiki 修复
- 侧边栏/反向引用链接相对路径错误（多了 `atom/` 前缀）→ 已修复全部50页
- 正文 wiki 链接跨目录缺 `../` → 已修复
- 3个新增概念页：IP与品牌、双花问题、元胞自动机

### Cron Status
- token-sync ✅, research-director ✅, lcm-sync ✅, soul-guardian ✅
- main: active, quantaalpha: last session 19:50 (~27m stale)
- Clash Verge proxy: alive on 7890/7891/7892
- EIA API via proxy: still failing（geo/upstream block，持续数天）
- Grid demand data: stale since Apr 17

## Heartbeat 22:17

### Cron Status (All OK @ 22:00)
- token-sync ✅, research-director ✅, lcm-sync ✅, soul-guardian ✅

### Agents
- main: active (session 22:17), context overflow diag on prior session (428 msgs), LLM timeouts/fallbacks observed
- quantaalpha: session 21:53 (24min stale), **embedded timeout at 21:02** - may need restart

### Self-Healing
- LLM timeouts on minimax/MiniMax-M2.7-highspeed: multiple failover events (18:30, 19:12, 20:05, 21:02). Model appears unstable this evening.
- Context overflow on main agent session (ovf-mo8ld2tg): 428 messages, compaction not attempted. Consider session reset.
- quantaalpha last embedded timeout at 21:02 → no session activity since → likely needs attention

### daemon
- mine_daemon: dead (expected, OOM since Apr 3)

## Heartbeat 23:17

### Cron Status (All OK @ 23:00)
- token-sync ✅, research-director ✅, lcm-sync ✅, soul-guardian ✅

### Agents
- main: active (session 23:17)
- quantaalpha: last session 22:53 (~24min stale), no new embedded run since

### Self-Healing
- Provider: minimax timeout spikes (18:30, 19:12, 20:05, 21:02 in logs) - pattern continues
- EIA/proxy: failing (geo block, known issue since days)
- Grid demand data: stale since Apr 17 (EIA direct fetch may be alternative)
- **interconnection data path broken**: `~/Document/QuantaAlpha/data/interconnection/` directory does not exist - quantaalpha cannot fetch this data until directory is created at deployment level

### daemon
- mine_daemon: dead (expected, OOM since Apr 3, no auto-restart)

### Desktop
- Clean (empty)
