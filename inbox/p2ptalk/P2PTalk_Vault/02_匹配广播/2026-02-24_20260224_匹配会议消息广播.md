---
subject: "20260224 匹配会议消息广播"
category: 匹配广播
thread_size: 4
first_date: 2026-02-24 13:40
last_date: 2026-02-24 15:52
participants: ["Woo", "initsysctrl", "沄涣", "胥亚朋"]
source: Google Groups p2ptalk
created: "2026-04-20"
tags:
  - p2ptalk
  - 匹配广播
---

# 20260224 匹配会议消息广播

**4 条消息** · 2026-02-24 → 2026-02-24
**分类：** 匹配广播

## 参与者
Woo | initsysctrl | 沄涣 | 胥亚朋

---

## 消息时间线

### 1. 沄涣 <jiyeyulie4b310@gmail.com>  ·  2026-02-24 13:40

0213会议停机后的问题整理
1.atom是提供给买家与节点使用的，如果不是P2P市场则没有这些问题
2.用户与节点可以随时加入退出，节点只有长时间在线，对于atom的数量计算的越准确
3.对于卖方已有的atom，可不可以更换出售的“商品”，此时卖方已有的atom数量能否作为“新商品”的声誉？这一点老史的停机称为“互联网摆摊”。
4.中本聪称每种交易类型都需要特殊的支持代码和数据字段，交易双方可以将他们的交易描述为一个谓词，谓词=脚本

匹配内容发在此邮件下，匹配上了就发起会议链接

---

### 2. Woo <godfrey9802@gmail.com>  ·  2026-02-23 22:11
> ↩ 回复: `<CAMxxz9q=LwBjsDkYnb6abh4hY4fO-ArfGhLrejK-XbP3Df16yw@mail.gm`

胖总给的压力太大了，先闲聊找找工作状态

On Tuesday, February 24, 2026 at 1:40:25 PM UTC+8 胡志伟 wrote:

> 0213会议停机后的问题整理
> 1.atom是提供给买家与节点使用的，如果不是P2P市场则没有这些问题
> 2.用户与节点可以随时加入退出，节点只有长时间在线，对于atom的数量计算的越准确
> 3.对于卖方已有的atom，可不可以更换出售的“商品”，此时卖方已有的atom数量能否作为“新商品”的声誉？这一点老史的停机称为“互联网摆摊”。
> 4.中本聪称每种交易类型都需要特殊的支持代码和数据字段，交易双方可以将他们的交易描述为一个谓词，谓词=脚本
>
> 匹配内容发在此邮件下，匹配上了就发起会议链接
>

---

### 3. initsysctrl <initsysctrl@gmail.com>  ·  2026-02-24 15:48
> ↩ 回复: `<CAMxxz9q=LwBjsDkYnb6abh4hY4fO-ArfGhLrejK-XbP3Df16yw@mail.gm`

<html>
<head>
    <meta http-equiv='Content-Type' content='text/html; charset=UTF-8'>
</head>
<body>
<style>
    font{
        line-height: 1.6;
    }
    ul,ol{
        padding-left: 20px;
        list-style-position: inside;
    }
</style>
<div style = 'font-family:Helvetica,Helvetica,微软雅黑, 宋体; line-height:1.6;'>
    <div ></div><div>
    <div>
        <strong><span style="font-family: Helvetica;">## 1. 不同的消息类型对应不同的传播策略<br></span></strong><br>在 bitcoin 0.1.0 中消息频道类型分为五种：<br><br>//net. h<br>enum {<br> &nbsp;MSG_TX = 1,//普通交易消息<br> &nbsp;MSG_BLOCK, // 区块消息<br> &nbsp;MSG_REVIEW, // 评论消息<br> &nbsp;MSG_PRODUCT, // 商品消息<br> &nbsp;MSG_TABLE, // 表格消息，结构化数据<br>};<br><br>分别是：<br>- MSG_TX 普通交易 <br>- MSG_BLOCK 区块消息<br>- MSG_REVIEW 评论消息<br>- MSG_PRODUCT 商品消息<br>- MSG_TABLE 结构化数据消息<br><br>不同的消息类型使用不同的传播策略：<br>其中普通交易、区块消息、评论消息都有完整的实现，它们都是使用泛洪传播，而商品消息和结构化数据消息使用中间会面机制传播。采用不同的传播机制，是根据实际情况来决策的。<br><br>交易、区块、评论，需要全网共识的数据，数据量相对较小，需要快速传播，需要覆盖 100%的节点集合，因此采用泛洪传播机制。<br>商品、其他结构化消息，不需要全网共识，数据量可能比较大，允许节点选择性接收，允许一部分节点不接受，因此采用中间会面机制传播。<br><br><strong>## 2. atom 不传播<br></strong>atom 本身只是一个无意义的短整数，atom 的个数用于评估不同 CUser 的信誉度或者活跃度。<br>atom 本身是不传播的，在 p 2 p 网络中，不存在针对 atom 的消息类型，因此 atom 的传播不适用于泛洪、中间会面等任意一种传播策略。<br><br>atom 系统有自己的流转策略，但这只是 cuser 和 cuser 中间的 atom 传播，而非 node 与 node 之间的消息传播。<br><br><strong>## 3. 谓词如果存在，应该在 Review 里而不在 atom 里面<br></strong>推理过程如下：<br>1、中本聪喜欢用某些局部关键字来代指广义上的某种技术，比如他在邮件中喜欢用'带宽'来代指存储量。中本聪称每种交易类型都需要特殊的支持代码和数据字段，交易双方可以将他们的交易描述为一个谓词。谓词本身是形式语言的一个关键字，其本身是表达"对象 A是什么"或者"对象 A 满足什么性质"。因此，谓词在这里应该指形式化语言。<br><br>2、比特币的 Tx 消息包含两个关键信息，比特币数量，和脚本。因此对于 Tx 消息类型而言，谓词就是形式语言，即脚本。<br><br>3、如果两种不同的消息，在 p 2 p 网络中，使用同一种传播策略，那么有理由推理，这两种不同的消息应该符合某一种相同的性质。上文已知，atom 是不传播的，它不属于任意一种消息类型。上面找到了的五种消息类型中，MSG_TX、MSG_BLOCK、MSG_REVIEW 都使用泛洪传播，如果 MSG_TX 的'谓词'是脚本，那么MSG_BLOCK 和MSG_REVIEW 都应该有它们自己的"谓词"。因为我们目前考虑的是 atom 系统，而与 atom 系统息息相关的消息类型就是MSG_REVIEW，因此如果谓词存在，如果谓词需要存在，只会存在MSG_REVIEW 中，即可推导出一个新的命题："review的谓词是什么"<br><br>4、分析 CReview 的数据结构：<br>```cpp<br>/////market. h<br>class CReview {<br>public:<br>int nVersion; // 评论版本号，默认值为1<br>uint256 hashTo; // 评论目标哈希值<br>map mapValue; // 评论内容键值对<br>vector vchPubKeyFrom; // 评论者公钥<br>vector vchSig; // 评论者签名<br>// memory only（仅在内存中使用，不写入数据库）<br>unsigned int nTime; // 评论时间戳，默认值为0<br>int nAtoms; // 评论中包含的原子数量，默认值为0<br>}<br>```<br><br>Creview 的结构比较简单，除了必要的评论对象 (hashTo) 和评论内容（mapValue）之外，评论者的公钥vchPubKeyFrom和签名vchSig是一对。<br>但需要注意，CReview 中包含了含的原子数量(nAtoms), 这个数据是节点本地计算使用的（内存中），接收 MSG_REVIEW 时，并没有 atom 的相关信息。<br><br>5、在main.cpp 中找到 review 消息的处理流程<br>```cpp<br>// main.cpp#L1863-L1881<br>else if (strCommand == "review") {<br> &nbsp;// 保存原始消息流，用于转发<br> &nbsp;CDataStream vMsg(vRecv);<br><br> &nbsp;// 从消息流中读取评论数据<br> &nbsp;CReview review;<br> &nbsp;vRecv &gt;&gt; review;<br><br> &nbsp;// 创建评论库存项<br> &nbsp;CInv inv(MSG_REVIEW, review.GetHash());<br> &nbsp;pfrom-&gt;AddInventoryKnown(inv); &nbsp;// 标记为已知，避免重复处理<br><br> &nbsp;// 尝试接受评论<br> &nbsp;// AcceptReview会验证评论的有效性，包括签名、内容等<br> &nbsp;if (review.AcceptReview()) {<br> &nbsp; &nbsp;// 转发原始消息，保持原始格式<br> &nbsp; &nbsp;RelayMessage(inv, vMsg);<br> &nbsp; &nbsp;// 从已请求列表中移除，避免重复请求<br> &nbsp; &nbsp;mapAlreadyAskedFor.erase(inv);<br> &nbsp;}<br>}<br>```<br><br>从 Review 的消息处理流程代码中可以发现，Review 的谓词在 `AcceptReview` 函数中。同时，需要注意的是 `RelayMessage` 函数，这意味着，一旦节点接收到 review 消息，它也会转发到其他邻近节点，这符合泛洪传播机制。<br><br>6、接下来查看关键函数 `AcceptReview` ：<br>// market. cpp<br>```cpp<br>bool CReview::AcceptReview() {<br>// - 为评论设置当前系统时间作为时间戳，记录评论的创建时间<br>nTime = GetTime();<br>// - 验证评论者的公钥是否与签名匹配，确保评论的真实性<br>if (!CKey::Verify(vchPubKeyFrom, GetSigHash(), vchSig))<br>return false;<br>// - 读取数据库中的已有评论，将当前评论添加到被评分用户的评论列表中<br>CReviewDB reviewdb;<br>// Add review text to recipient<br><br>// 数据库初始化：创建评论数据库访问对象，用于后续的读写操作<br><br>vector vReviews;<br><br>// 首先读取目标对象（ hashTo ）的现有评论列表<br><br>reviewdb.ReadReviews(hashTo, vReviews);<br><br>//- 将当前评论添加到评论列表中<br><br>vReviews.push_back(*this);<br><br>//- - 将更新后的评论列表写回数据库<br><br>if (!reviewdb.WriteReviews(hashTo, vReviews))<br><br>return false;<br><br> &nbsp;<br><br>// 创建一个评分者和被评分者的连接关系，Link<br><br>CUser user;<br><br>// 通过评论者公钥计算用户哈希（ hashFrom<br><br>// ），用于在数据库中查找评论者的用户信息<br><br>uint256 hashFrom = Hash(vchPubKeyFrom.begin(), vchPubKeyFrom.end());<br><br>//- 数据库中读取评论者的用户数据<br><br>reviewdb.ReadUser(hashFrom, user);<br><br>// 在评论者的 vLinksOut 列表中添加指向被评论对象的链接<br><br>user.vLinksOut.push_back(hashTo);<br><br>//- - 写回更新后的用户数据<br><br>if (!reviewdb.WriteUser(hashFrom, user))<br><br>	return false;<br><br> &nbsp;<br><br>reviewdb.Close(); // 关闭数据库<br><br> &nbsp;<br><br>// 最后一个关键步骤：将原子传播给被评论者<br><br>// Propagate atoms to recipient<br><br>vector vZeroAtom(1, 0); // 创建一个包含单个零原子(值为0)的向量<br><br> &nbsp;<br><br>// 调用 AddAtomsAndPropagate 函数将零原子或用户待传播原子传播给被评论者<br><br>// 这里出现零原子的情况是为了处理用户没有待传播原子的情况<br><br>// 优先使用评论者已有的声誉原子进行传播，这样可以传递评论者的信誉权重<br><br>//- - 当评论者没有声誉原子时，回退到使用零原子，这是一种优雅的降级处理<br><br>if (!AddAtomsAndPropagate(<br><br>hashTo, user.vAtomsOut.size() ? user.vAtomsOut : vZeroAtom, false))<br>	return false;<br>return true;<br><br>}<br>```<br>从上述代码看，关键在于 `CKey::Verify(vchPubKeyFrom, GetSigHash(), vchSig)` 这是一个通用的签名检查操作。虽然从语法上看，没有像 Tx 消息那样，使用脚本语言来作为"谓词"来描述"Tx 满足什么性质"，但是从语义上看，Rewive 的谓词等价于 Tx 的脚本中的 P 2 SK &nbsp;脚本类型。<br><br>上面的代码中，需要重申一遍关于 atom 的处理。review 消息在传播中，是不携带 atom 信息的，但是在节点收到 review 消息后，natom 参与了计算。这需要节点从本地中，找到评论发布者 cuser 的信息，然后使用 atom 的流转机制，这只发生在节点本地。<br><br>如果将 CReview 的结构做一点改变，将 `vchSig` 替换为 `vchScript`，那么理论上，这套系统依然可以运行，但似乎这样做没有什么意义。对于交易而言，我们可能需要复杂的交易脚本，但是对于评论而言，只需要满足签名正确就可以了。<br><br><span>
            
        </span>
    </div>
    <div>
        <span>
            <br>
        </span>
    </div>
    <div id="ntes-pcmac-signature" style="font-family:'Helvetica','Microsoft Yahei', '微软雅黑'">
    
    <div style="font-size:14px; padding: 0;  margin:0;">

    </div>
</div>
    <br>
</div><div class="J-reply ntes-mailmaster-quote" style="padding-top: 1px; padding-bottom: 1px">
  <div style="
      margin-top: 2em;
      margin-bottom: 1em;
      font-size: 14px;
      line-height: 1.25;
      color: #78787a;
    ">
    ---- 回复的原邮件 ----
  </div>
  <div style="
      margin

_(内容已截断)_

---

### 4. 胥亚朋 <xuyapeng110@gmail.com>  ·  2026-02-24 15:52
> ↩ 回复: `<FE8ECDDB-40CD-4FAF-886B-450FA6687B22@gmail.com>`

过完年第一天来上班，急需老史来帮忙找下工作状态。计算内容还需要一点时间来沉淀一下。

initsysctrl <initsysctrl@gmail.com> 于 2026年2月24日周二 15:48写道：

>
> *## 1. 不同的消息类型对应不同的传播策略*
> 在 bitcoin 0.1.0 中消息频道类型分为五种：
>
> //net. h
> enum {
>  MSG_TX = 1,//普通交易消息
>  MSG_BLOCK, // 区块消息
>  MSG_REVIEW, // 评论消息
>  MSG_PRODUCT, // 商品消息
>  MSG_TABLE, // 表格消息，结构化数据
> };
>
> 分别是：
> - MSG_TX 普通交易
> - MSG_BLOCK 区块消息
> - MSG_REVIEW 评论消息
> - MSG_PRODUCT 商品消息
> - MSG_TABLE 结构化数据消息
>
> 不同的消息类型使用不同的传播策略：
>
> 其中普通交易、区块消息、评论消息都有完整的实现，它们都是使用泛洪传播，而商品消息和结构化数据消息使用中间会面机制传播。采用不同的传播机制，是根据实际情况来决策的。
>
> 交易、区块、评论，需要全网共识的数据，数据量相对较小，需要快速传播，需要覆盖 100%的节点集合，因此采用泛洪传播机制。
> 商品、其他结构化消息，不需要全网共识，数据量可能比较大，允许节点选择性接收，允许一部分节点不接受，因此采用中间会面机制传播。
>
>
> *## 2. atom 不传播*atom 本身只是一个无意义的短整数，atom 的个数用于评估不同 CUser 的信誉度或者活跃度。
> atom 本身是不传播的，在 p 2 p 网络中，不存在针对 atom 的消息类型，因此 atom 的传播不适用于泛洪、中间会面等任意一种传播策略。
>
> atom 系统有自己的流转策略，但这只是 cuser 和 cuser 中间的 atom 传播，而非 node 与 node 之间的消息传播。
>
>
> *## 3. 谓词如果存在，应该在 Review 里而不在 atom 里面*推理过程如下：
> 1、中本聪喜欢用某些局部关键字来代指广义上的某种技术，比如他在邮件中喜欢用'带宽'来代指存储量。中本聪称每种交易类型都需要特殊的支持代码和数据字段，交易双方可以将他们的交易描述为一个谓词。谓词本身是形式语言的一个关键字，其本身是表达"对象
> A是什么"或者"对象 A 满足什么性质"。因此，谓词在这里应该指形式化语言。
>
> 2、比特币的 Tx 消息包含两个关键信息，比特币数量，和脚本。因此对于 Tx 消息类型而言，谓词就是形式语言，即脚本。
>
> 3、如果两种不同的消息，在 p 2 p 网络中，使用同一种传播策略，那么有理由推理，这两种不同的消息应该符合某一种相同的性质。上文已知，atom
> 是不传播的，它不属于任意一种消息类型。上面找到了的五种消息类型中，MSG_TX、MSG_BLOCK、MSG_REVIEW 都使用泛洪传播，如果
> MSG_TX 的'谓词'是脚本，那么MSG_BLOCK 和MSG_REVIEW 都应该有它们自己的"谓词"。因为我们目前考虑的是 atom 系统，而与
> atom 系统息息相关的消息类型就是MSG_REVIEW，因此如果谓词存在，如果谓词需要存在，只会存在MSG_REVIEW
> 中，即可推导出一个新的命题："review的谓词是什么"
>
> 4、分析 CReview 的数据结构：
> ```cpp
> /////market. h
> class CReview {
> public:
> int nVersion; // 评论版本号，默认值为1
> uint256 hashTo; // 评论目标哈希值
> map mapValue; // 评论内容键值对
> vector vchPubKeyFrom; // 评论者公钥
> vector vchSig; // 评论者签名
> // memory only（仅在内存中使用，不写入数据库）
> unsigned int nTime; // 评论时间戳，默认值为0
> int nAtoms; // 评论中包含的原子数量，默认值为0
> }
> ```
>
> Creview 的结构比较简单，除了必要的评论对象 (hashTo)
> 和评论内容（mapValue）之外，评论者的公钥vchPubKeyFrom和签名vchSig是一对。
> 但需要注意，CReview 中包含了含的原子数量(nAtoms), 这个数据是节点本地计算使用的（内存中），接收 MSG_REVIEW 时，并没有
> atom 的相关信息。
>
> 5、在main.cpp 中找到 review 消息的处理流程
> ```cpp
> // main.cpp#L1863-L1881
> else if (strCommand == "review") {
>  // 保存原始消息流，用于转发
>  CDataStream vMsg(vRecv);
>
>  // 从消息流中读取评论数据
>  CReview review;
>  vRecv >> review;
>
>  // 创建评论库存项
>  CInv inv(MSG_REVIEW, review.GetHash());
>  pfrom->AddInventoryKnown(inv);  // 标记为已知，避免重复处理
>
>  // 尝试接受评论
>  // AcceptReview会验证评论的有效性，包括签名、内容等
>  if (review.AcceptReview()) {
>    // 转发原始消息，保持原始格式
>    RelayMessage(inv, vMsg);
>    // 从已请求列表中移除，避免重复请求
>    mapAlreadyAskedFor.erase(inv);
>  }
> }
> ```
>
> 从 Review 的消息处理流程代码中可以发现，Review 的谓词在 `AcceptReview` 函数中。同时，需要注意的是
> `RelayMessage` 函数，这意味着，一旦节点接收到 review 消息，它也会转发到其他邻近节点，这符合泛洪传播机制。
>

---
